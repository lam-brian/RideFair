const MenuBar = () => {
  return (
    <div>
      <div>
        <button>Home</button>
        <button>Activity</button>
        <button>Profile</button>
      </div>
    </div>
  );
};

export default MenuBar;
